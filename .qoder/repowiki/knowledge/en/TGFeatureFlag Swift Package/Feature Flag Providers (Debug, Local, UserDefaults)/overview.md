Concrete implementations of the FeatureFlagProvider protocol that source feature flag values from in-memory dictionaries, mutable debug overrides, UserDefaults, and percentage-based rollout. The AsyncFeatureFlagProvider protocol adds async `fetchValues()` for remote config providers (e.g., Firebase Remote Config). RolloutProvider enables deterministic percentage-based rollouts using a stable hash of userId + flag key.